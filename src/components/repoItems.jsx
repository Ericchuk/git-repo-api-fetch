// import {useParams} from 'react-router-dom'

export default function RepoItems(){
    return(
        <section>
            {/* let { repoId } = useParams(); */}
            console.log(repoId)
        </section>
    )
}